# K9s 스타일 TUI 개선 계획

## 개요

기존 `agent-collab dashboard` TUI를 k9s 스타일로 개선하여, 단축키로 명령어를 직접 실행할 수 있는 인터랙티브 TUI를 구현합니다.

## 현재 상태 분석

### 기존 TUI 구조 (`internal/interface/tui/`)
```
tui/
├── app.go          # 앱 초기화, Init(), Option 패턴
├── model.go        # Model 정의, 5개 탭 (Cluster, Context, Locks, Tokens, Peers)
├── view.go         # 렌더링 로직
├── update.go       # 메시지 처리 (키 입력, 데이터 갱신)
├── messages.go     # 메시지 타입 정의
├── keys.go         # KeyMap 정의
├── styles.go       # Lipgloss 스타일
├── components/     # 재사용 컴포넌트 (header, tabs, footer, table, gauge, sparkline)
└── views/          # 탭별 뷰 (cluster, context, locks, tokens, peers)
```

### 기존 단축키
- `q` - 종료
- `r` - 새로고침
- `1-5` - 탭 전환
- `Tab/Shift+Tab` - 다음/이전 탭
- `↑↓/jk` - 네비게이션
- `Enter` - 선택
- `Esc` - 취소
- `?` - 도움말

### CLI 명령어 (TUI에서 실행 가능하게 할 대상)
- `init` - 새 클러스터 초기화
- `join <token>` - 클러스터 참여
- `leave` - 클러스터 탈퇴
- `status` - 상태 확인
- `lock list/release/history` - 락 관리
- `agents list/info/providers` - 에이전트 관리
- `peers` - 피어 목록
- `token` - 토큰 사용량
- `config` - 설정 관리

---

## 개선 계획

### Phase 1: 모드 시스템 도입

TUI에 세 가지 모드를 추가합니다:

1. **Normal 모드** (기본)
   - 현재처럼 탭 네비게이션 및 데이터 조회

2. **Command 모드** (`:` 키로 진입)
   - k9s 스타일 명령 팔레트
   - 텍스트 입력으로 명령 실행

3. **Action 모드** (단축키로 진입)
   - 선택된 항목에 대한 액션 실행
   - 컨텍스트 메뉴 표시

### Phase 2: 단축키 확장

#### Global 단축키 (모든 모드에서 동작)
| 키 | 설명 | 동작 |
|---|---|---|
| `q` | 종료 | 프로그램 종료 |
| `:` | 명령 모드 | 명령 팔레트 열기 |
| `?` | 도움말 | 도움말 표시 |
| `Esc` | 취소 | Normal 모드로 복귀 |

#### Action 단축키 (Normal 모드에서)
| 키 | 명령 | 설명 |
|---|---|---|
| `i` | init | 새 클러스터 초기화 |
| `j` | join | 클러스터 참여 (토큰 입력 프롬프트) |
| `L` | leave | 클러스터 탈퇴 (확인 프롬프트) |
| `s` | status | 상태 상세 보기 |
| `a` | agents | 에이전트 목록 |
| `p` | peers | 피어 목록 |
| `l` | locks | 락 목록 |
| `t` | tokens | 토큰 사용량 |
| `c` | config | 설정 보기 |
| `r` | refresh | 새로고침 |

#### 컨텍스트 단축키 (선택된 항목에 대해)
| 키 | 탭 | 설명 |
|---|---|---|
| `Enter` | 모두 | 상세 보기 |
| `d` | Locks | 락 해제 (delete) |
| `y` | 모두 | YAML 복사 |
| `e` | Config | 편집 |

### Phase 3: 파일 구조 변경

```
tui/
├── app.go              # 앱 초기화 (수정)
├── model.go            # Model 확장 (mode, input 추가)
├── view.go             # 렌더링 (모드별 분기)
├── update.go           # 메시지 처리 확장
├── messages.go         # 메시지 타입 추가
├── keys.go             # KeyMap 확장
├── styles.go           # 스타일 추가
│
├── mode/               # [신규] 모드별 처리
│   ├── mode.go         # Mode 타입 정의
│   ├── normal.go       # Normal 모드 핸들러
│   ├── command.go      # Command 모드 핸들러
│   └── action.go       # Action 모드 핸들러
│
├── command/            # [신규] 명령 실행
│   ├── executor.go     # 명령 실행 인터페이스
│   ├── init.go         # init 명령
│   ├── join.go         # join 명령
│   ├── leave.go        # leave 명령
│   ├── status.go       # status 명령
│   ├── lock.go         # lock 명령
│   └── agents.go       # agents 명령
│
├── input/              # [신규] 입력 처리
│   ├── prompt.go       # 텍스트 입력 프롬프트
│   ├── confirm.go      # 확인 대화상자
│   └── selector.go     # 선택 대화상자
│
├── components/         # 기존 컴포넌트
│   ├── header.go
│   ├── tabs.go
│   ├── footer.go       # (수정) 모드별 키 힌트
│   ├── table.go
│   ├── gauge.go
│   ├── sparkline.go
│   ├── command_palette.go  # [신규] 명령 팔레트
│   └── status_bar.go       # [신규] 상태 바
│
└── views/              # 기존 뷰
    ├── cluster.go
    ├── context.go
    ├── locks.go        # (수정) 선택 및 액션
    ├── tokens.go
    └── peers.go        # (수정) 선택 및 액션
```

### Phase 4: 구현 상세

#### 4.1 Mode 타입 정의 (`mode/mode.go`)
```go
type Mode int

const (
    ModeNormal Mode = iota
    ModeCommand
    ModeAction
    ModeInput
    ModeConfirm
)
```

#### 4.2 Model 확장 (`model.go`)
```go
type Model struct {
    // 기존 필드...

    // 모드
    mode        Mode
    prevMode    Mode

    // 명령 팔레트
    commandInput  textinput.Model
    commandHints  []CommandHint

    // 입력 프롬프트
    inputPrompt   string
    inputCallback func(string) tea.Cmd

    // 확인 대화상자
    confirmPrompt string
    confirmAction func() tea.Cmd

    // 선택
    selectedIndex int
    selectedItem  interface{}

    // 실행 결과
    lastResult    string
    lastError     error
}
```

#### 4.3 Command Executor (`command/executor.go`)
```go
type Executor interface {
    Execute(ctx context.Context, args []string) (string, error)
    Name() string
    Description() string
    Usage() string
}

type CommandRegistry struct {
    commands map[string]Executor
}

func (r *CommandRegistry) Execute(ctx context.Context, input string) tea.Cmd {
    // 명령 파싱 및 실행
}
```

#### 4.4 Command Palette UI
```
┌─────────────────────────────────────────────────────────────┐
│ :init -p my-project                                         │
├─────────────────────────────────────────────────────────────┤
│   init      새 클러스터 초기화                              │
│   join      클러스터 참여                                   │
│   leave     클러스터 탈퇴                                   │
│   status    상태 확인                                       │
│   lock      락 관리                                         │
│   agents    에이전트 관리                                   │
│   config    설정 관리                                       │
└─────────────────────────────────────────────────────────────┘
```

#### 4.5 Footer 개선 (모드별 키 힌트)

**Normal 모드:**
```
[q] Quit  [r] Refresh  [:] Command  [i] Init  [j] Join  [L] Leave  [?] Help
```

**Command 모드:**
```
[Enter] Execute  [Tab] Autocomplete  [Esc] Cancel  [↑↓] History
```

**Locks 탭 (선택 상태):**
```
[Enter] Detail  [d] Release  [y] Copy  [Esc] Cancel
```

---

## 구현 순서

### Step 1: 모드 시스템 기반 구축
1. `mode/mode.go` - Mode 타입 정의
2. `model.go` 수정 - mode 필드 추가
3. `update.go` 수정 - 모드별 키 처리 분기
4. `view.go` 수정 - 모드별 렌더링 분기

### Step 2: Command Palette 구현
1. `components/command_palette.go` - 명령 팔레트 UI
2. `command/executor.go` - 명령 실행 인터페이스
3. `mode/command.go` - Command 모드 핸들러
4. 자동완성 및 히스토리 지원

### Step 3: Action 단축키 구현
1. `keys.go` 확장 - 액션 키 추가
2. `mode/action.go` - 액션 핸들러
3. 각 명령 구현 (`command/init.go`, `command/join.go` 등)

### Step 4: 입력/확인 대화상자
1. `input/prompt.go` - 텍스트 입력
2. `input/confirm.go` - 확인 대화상자
3. `input/selector.go` - 선택 대화상자

### Step 5: 탭별 선택 및 액션
1. `views/locks.go` 수정 - 락 선택 및 해제
2. `views/peers.go` 수정 - 피어 선택 및 상세 보기
3. 컨텍스트 메뉴 구현

### Step 6: 실시간 데이터 연동
1. daemon 연동 (현재 TODO로 되어있는 부분)
2. WebSocket 또는 Unix socket 통신
3. 실시간 업데이트

### Step 7: 테스트 및 문서화
1. 단위 테스트
2. 통합 테스트
3. 사용자 가이드

---

## 예상 결과물

### 실행 화면 예시

```
🔗 agent-collab                Project: my-project | Node: QmXx...Yy
Status: ● Connected           Peers: 4 | Sync: 98.5%              Uptime: 2h 34m
────────────────────────────────────────────────────────────────────────────────
[1] Cluster  [2] Context  [3] Locks  [4] Tokens  [5] Peers
────────────────────────────────────────────────────────────────────────────────
╭──────────────────────────────────────────────────────────────────────────────╮
│ Cluster Overview                                                             │
│                                                                              │
│ Health Score: ████████████████████ 98.5%  [Healthy]                          │
│                                                                              │
│ Quick Stats                                                                  │
│   Total Peers      : 4                                                       │
│   Active Locks     : 2                                                       │
│   Pending Syncs    : 0                                                       │
│   Avg Latency      : 42ms                                                    │
│   Messages/sec     : 12.4                                                    │
╰──────────────────────────────────────────────────────────────────────────────╯
────────────────────────────────────────────────────────────────────────────────
[q] Quit  [r] Refresh  [:] Command  [i] Init  [j] Join  [L] Leave  [?] Help
CPU: 2.3% | MEM: 45.0MB | NET: ↑12.0KB/s ↓8.0KB/s | Tokens: 1.2K/hr
```

### 명령 팔레트 예시 (`:` 입력 시)

```
╭─────────────────────────────────────────────────────────────────────────────╮
│ :join█                                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│ ▸ join <token>     클러스터 참여                                            │
│   init             새 클러스터 초기화                                       │
│   leave            클러스터 탈퇴                                            │
│   status           상태 확인                                                │
│   lock list        락 목록                                                  │
│   lock release     락 해제                                                  │
╰─────────────────────────────────────────────────────────────────────────────╯
```

---

## 평가 기준

1. **사용성**: k9s와 유사한 직관적인 UX
2. **완성도**: 모든 CLI 명령어를 TUI에서 실행 가능
3. **반응성**: 빠른 키 입력 응답
4. **안정성**: 명령 실행 중 오류 처리
5. **확장성**: 새 명령 추가 용이

---

## 리스크 및 대응

| 리스크 | 대응 방안 |
|--------|----------|
| 기존 TUI 코드와 충돌 | 별도 브랜치(worktree)에서 작업, 점진적 통합 |
| bubbletea 제약 | bubbles 라이브러리 활용 (textinput, list, viewport) |
| 실시간 데이터 연동 복잡도 | 먼저 목업 데이터로 UI 완성 후 연동 |
| 명령 실행 시 블로킹 | tea.Cmd로 비동기 실행 |

---

## 타임라인 (예상)

- Step 1-2: 모드 시스템 + 명령 팔레트 기반
- Step 3-4: 액션 단축키 + 입력 대화상자
- Step 5-6: 탭별 액션 + 실시간 연동
- Step 7: 테스트 및 문서화

---

## 다음 단계

이 계획에 대한 검토 후:
1. 피드백 반영
2. 승인 시 구현 시작 (Step 1부터)
